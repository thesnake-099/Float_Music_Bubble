<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:elevation="12dp"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <!-- 1. Left Resize Handle (bottom-left corner) -->
    <ImageView
        android:id="@+id/resizeHandleBottomLeft"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:src="@drawable/ic_bottom_left"
         />

    <View
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- 2. Center Move Handle -->
    <ImageView
        android:id="@+id/moveHandle"
        android:layout_width="84dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_mover" />

    <View
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- 3. Right Resize Handle (bottom-right corner) -->
    <ImageView
        android:id="@+id/resizeHandleBottomRight"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:rotation="225"
        android:src="@drawable/ic_bottom_right" />
</LinearLayout>